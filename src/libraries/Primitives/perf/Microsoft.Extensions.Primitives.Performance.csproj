<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>$(NetCoreAppCurrent);net472</TargetFrameworks>
    <Configurations>$(NetCoreAppCurrent)-Debug;$(NetCoreAppCurrent)-Release;net472-Debug;net472-Release</Configurations>
    <!-- <TargetFramework>$(DefaultNetCoreTargetFramework)</TargetFramework>
    <Configurations>$(DefaultNetCoreTargetFramework)-Debug;$(DefaultNetCoreTargetFramework)-Release</Configurations> -->
    <OutputType>Exe</OutputType>
    <ServerGarbageCollection>true</ServerGarbageCollection>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <Reference Include="Microsoft.Extensions.Primitives" />
  </ItemGroup>

  <ItemGroup>
    <Compile Include="$(SharedSourceRoot)BenchmarkRunner\**\*.cs">
      <Link>Shared\%(FileName)%(Extension)</Link>
    </Compile>
  </ItemGroup>

  <ItemGroup>
    <Reference Include="BenchmarkDotNet" />
    <Reference Include="Newtonsoft.Json" />
  </ItemGroup>

</Project>
